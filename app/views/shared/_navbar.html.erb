<nav class="navbar navbar-expand-lg px-5" style="background-color: #FDDA0D	;">
  <div class="container-fluid">
    <a class="navbar-brand" href="/jobs">Jobs Board</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <% if current_user %>    
        <li class="nav-item dropdown">          
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Hello               
            <span class="owner-avatar"><%= check_photo(current_user, 25, 25) %> </span> 
              
             <%= current_user.name %>
            
          </a>

          <ul class="dropdown-menu">          
            <li><a class="nav-link" href="/user/profile">Your profile</a></li>
            <li><a class="nav-link" href="/users/edit">Edit your profile</a></li>
            <li><a class="nav-link" href="/applications">Your applications</a></li>          
            <li><%= button_to 'Log out', destroy_user_session_path, class:
            'dropdown-item', method: :delete %></li>   
          </ul>
             
              
        </li>
              <% end %>
              <%# <span class="owner-avatar">    %>
                  
              <%#= image_tag 'default_avatar.jpg', class:'rounded-circle',  style:"height:30px; width:30px" %>
          
              <%# <span class="owner-avatar">    %>
              <%#= image_tag current_user.photo, class:'rounded-circle',  style:"height:30px; width:30px" %>
           
      </ul>
    
    <% if current_user %>
     <% if current_user.role == "admin" %>
        <div class="d-grid gap-2 d-md-flex justify-content-md-end">      
        <button type="button" class="btn btn-success position-relative me-3">
          Received Applications 
          <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"><%= @apps %>
          <span class="visually-hidden">unread messages</span></span>
        </button>     
        <%= link_to "Add new job", new_job_path, class: "btn btn-success" %>
        </div>    
      <% end %>
    <% end %>
  </div>
</nav>